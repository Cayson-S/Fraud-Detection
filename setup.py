from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='The project fufills the requirements set by the data science capstone. It looks to predict fraud from transaction data.',
    author='Cayson Seipel',
    license='',
)
